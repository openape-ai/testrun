{
  "name": "openape-testrun-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": ["app", "cli"],
  "scripts": {
    "dev": "pnpm --filter app dev",
    "build": "pnpm --filter app build",
    "cli:build": "pnpm --filter cli build",
    "deploy:image": "node scripts/deploy-image.mjs",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.29.3"
}
